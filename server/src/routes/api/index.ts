import { Router } from 'express';
//imports for /api/routes here

const router = Router();

// authenticate token for just journalEnry routes? maybe...
// router.use('users', userRouter); 
// router.use('journalEntries', journalEntryRouter);

export default router;